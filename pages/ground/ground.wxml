<!--pages/ground/ground.wxml-->
<view class="searchBar">
  <image class="icon" src="../../icons/search.png"></image>
  <input class="searchIN" confirm-type="search" placeholder="搜索" placeholder-class="searchPlaceHolder" bindconfirm="search"></input>
</view>

<!-- 导航栏 -->
<view class="navbar">
  <text wx:for="{{navbar}}" data-index="{{index}}" wx:key="index" class="item {{currentTab==index ? 'active' : ''}}" bindtap="navbarTap">{{item.text}}</text>
</view>


<!-- 活动发布 -->
<scroll-view wx:if="{{currentTab==0}}">
  <!--添加查看详情事件-->
  <view wx:for="{{activity}}" wx:key="a_id" bindtap="onTapseeDetails">
    <view class="title">
      <view class="poster">
        <image class="icon" src="{{item.u_photo}}"></image>
        <text class="name">{{item.u_name}}</text>
      </view>
      <view>
        <text class="time">{{item.time}}</text>
      </view>
    </view>
    <view class="content">
      <view style="display:flex;">
        <text>活动名称：</text>
        <text>{{item.activityName}}</text>
      </view>
      <view style="display:flex;">
        <text>活动内容：</text>
        <text>{{item.activityContent}}</text>
      </view>
    </view>
    <view class="operation">
      <image src="../../icons/message.png" bindtap="onTapMessage"></image>
      <image src="../../icons/send.png" bindtap="onTapSend"></image>
      <image src="../../icons/like.png" bindtap="onTapLike"></image>
    </view>
    <!--点赞者列表，点击跳转至othersInfo-->
    <view class="operationContent" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.liker}}" wx:key="likerID" data-id="{{likerID}}" bindtap="onTapLiker">{{item.likerName}}</view>
      <view>已点赞</view>
    </view>
    <!--转发者列表，点击跳转至othersInfo-->
    <view class="operationContent" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.sender}}" wx:key="senderID" data-id="{{senderID}}" bindtap="onTapSender">{{item.senderName}}</view>
      <view>已转发</view>
    </view>
    <!--评论详情列表，点击评论人名称跳转至othersInfo-->
    <view class="operationContent" style="justify-content:column;" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.comment}}" wx:key="commmentatorID" data-id="{{commmentatorID}}" bindtap="onTapCommentaor">
        <text>{{item.commentatorName}}</text>
        <text>：</text>
        <text>{{item.comment}}</text>
      </view>
    </view>
  </view>
</scroll-view>

<!-- 队友招募 -->
<scroll-view wx:elif="{{currentTab==1}}">
  <!--添加查看详情事件-->
  <view wx:for="{{recruitTeammates}}" wx:key="a_id" bindtap="seeDetails">
    <view class="title">
      <view class="poster">
        <image class="icon" src="{{item.u_photo}}"></image>
        <text class="name">{{item.u_name}}</text>
      </view>
      <view>
        <text class="time">{{item.time}}</text>
      </view>
    </view>
    <view class="content">
      <view style="display:flex;">
        <text>招募条件：</text>
        <text>{{item.recruitContent}}</text>
      </view>
    </view>
    <view class="operation">
      <image src="../../icons/message.png" bindtap="onTapMessage"></image>
      <image src="../../icons/send.png" bindtap="onTapSend"></image>
      <image src="../../icons/like.png" bindtap="onTapLike"></image>
    </view>
    <!--点赞者列表，点击跳转至othersInfo-->
    <view class="operationContent" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.liker}}" wx:key="likerID" data-id="{{likerID}}" bindtap="onTapLiker">{{item.likerName}}</view>
      <view>已点赞</view>
    </view>
    <!--转发者列表，点击跳转至othersInfo-->
    <view class="operationContent" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.sender}}" wx:key="senderID" data-id="{{senderID}}" bindtap="onTapSender">{{item.senderName}}</view>
      <view>已转发</view>
    </view>
    <!--评论详情列表，点击评论人名称跳转至othersInfo-->
    <view class="operationContent" style="justify-content:column;" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.comment}}" wx:key="commmentatorID" data-id="{{commmentatorID}}" bindtap="onTapCommentaor">
        <text>{{item.commentatorName}}</text>
        <text>：</text>
        <text>{{item.comment}}</text>
      </view>
    </view>
  </view>
</scroll-view>

<!-- 精彩瞬间 -->
<scroll-view wx:elif="{{currentTab==2}}">
  <!--添加查看详情事件-->
  <view wx:for="{{movement}}" wx:key="a_id" bindtap="seeDetails">
    <view class="title">
      <view class="poster">
        <image class="icon" src="{{item.u_photo}}"></image>
        <text class="name">{{item.u_name}}</text>
      </view>
      <view>
        <text class="time">{{item.time}}</text>
      </view>
    </view>
    <view class="content">
        <text>{{item.movementContent}}</text>
        <image src="{{item.attachedImg}}"></image>
    </view>
    <view class="operation">
      <image src="../../icons/message.png" bindtap="onTapMessage"></image>
      <image src="../../icons/send.png" bindtap="onTapSend"></image>
      <image src="../../icons/like.png" bindtap="onTapLike"></image>
    </view>
    <!--点赞者列表，点击跳转至othersInfo-->
    <view class="operationContent" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.liker}}" wx:key="likerID" data-id="{{likerID}}" bindtap="onTapLiker">{{item.likerName}}</view>
      <view>已点赞</view>
    </view>
    <!--转发者列表，点击跳转至othersInfo-->
    <view class="operationContent" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.sender}}" wx:key="senderID" data-id="{{senderID}}" bindtap="onTapSender">{{item.senderName}}</view>
      <view>已转发</view>
    </view>
    <!--评论详情列表，点击评论人名称跳转至othersInfo-->
    <view class="operationContent" style="justify-content:column;" wx:if="{{item.liker.length!=0}}">
      <view wx:for="{{item.comment}}" wx:key="commmentatorID" data-id="{{commmentatorID}}" bindtap="onTapCommentaor">
        <text>{{item.commentatorName}}</text>
        <text>：</text>
        <text>{{item.comment}}</text>
      </view>
    </view>
  </view>
</scroll-view>